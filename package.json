{
  "name": "j-connect-global",
  "private": true,
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "validate": "node scripts/validate-all.mjs",
    "validate:all": "node scripts/validate-all.mjs --build --with-browser",
    "validate:production": "node scripts/validate-production-parity.mjs",
    "test:browser": "playwright test --config=playwright.config.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.61.1"
  }
}
